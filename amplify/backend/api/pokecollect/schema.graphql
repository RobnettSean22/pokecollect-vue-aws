type Bins @model @auth({allow: owner}) {
  id: ID!
  owner: ID!
  ownerId: String!
  title: String!
  createAt: String
  updateAt: String
  pokemoncards: [Photo] @connection
}

type Pokemon @model {
  id: ID!
  name: String!
}
